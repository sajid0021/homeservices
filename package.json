{
  "name": "quantira-backend",
  "version": "1.0.0",
  "description": "Service Provider Backend Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -r frontend/dist/. dist/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "helmet": "^7.1.0",
    "twilio": "^6.0.2"
  }
}
